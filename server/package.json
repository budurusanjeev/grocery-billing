{
  "name": "groci-server",
  "version": "1.0.0",
  "description": "Tiny backend for the grocery-billing app: keeps the Gemini API key server-side for the scan feature.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
